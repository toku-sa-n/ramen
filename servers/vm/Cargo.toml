[package]
name = "vm"
version = "0.1.0"
edition = "2018"

[lib]
name = "vm"
crate-type = ["staticlib"]
test = false
bench = false

[dependencies]
linked_list_allocator = "0.9.0"
ralib = { path = "../../libs/ralib", default-features = false }
